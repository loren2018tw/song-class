## 1. 白板元件分層與型別調整

- [x] 1.1 在 `src/components/WhiteboardCanvas.vue` 將單一 canvas 改為背景層與塗鴉層雙 canvas 疊合，保留既有 1280×720 座標與縮放行為。
- [x] 1.2 在 `src/types/whiteboard.ts` 擴充白板快照型別，使背景資料與筆畫資料分離且向後相容。
- [x] 1.3 在白板元件加入父元件可傳入背景圖片資料的輸入契約，並實作背景層重繪流程（含載入失敗回退）。

## 2. 繪圖與橡皮擦行為更新

- [x] 2.1 將橡皮擦邏輯改為在塗鴉層使用 `destination-out` 擦除，確保不影響背景層。
- [x] 2.2 調整重繪與清除流程，確保「清除畫布」僅清除塗鴉層並保留背景層。
- [x] 2.3 更新快照輸出與還原邏輯，驗證背景變更、塗鴉、擦除後可重現一致畫面。

## 3. 教師端背景選單整合

- [x] 3.1 在 `src/views/TeacherView.vue` 串接右側「教師背景」下拉選單與白板元件背景輸入。
- [x] 3.2 確保背景選項切換與清空時，白板背景層即時更新且不覆蓋現有塗鴉。

## 4. 驗證與回歸檢查

- [x] 4.1 驗證教師端操作流程：切換背景、繪圖、橡皮擦、清除塗鴉，結果符合新規格。
- [x] 4.2 驗證既有縮放與遠端還原情境未退化（含舊快照缺少背景欄位時的相容行為）。
